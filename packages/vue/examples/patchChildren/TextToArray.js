import { h, ref } from "../../lib/guide-mini-vue.esm.js"

export const TextToArray = {
  render() {
    return h(
      'div', 
      {},
      [
        h('div', {}, 'TextToArray'),
      ]
    )
  },

  setup() {
    return {}
  }
}